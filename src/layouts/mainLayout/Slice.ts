import { 
  createSlice,
  // PayloadAction,
} from '@reduxjs/toolkit';

const initialState = {
};

export const Slice = createSlice({
  name: 'mainLayout',
  initialState,
  reducers: {
  },
});

export default Slice.reducer;
